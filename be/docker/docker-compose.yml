services:
  db:
    image: postgres:18.3
    restart: always
    ports:
      - 5432:5432
    environment:
      - POSTGRES_user=postgres
      - POSTGRES_PASSWORD=postgres
    volumes:
      - ./postgres/init:/docker-entrypoint-initdb.d
